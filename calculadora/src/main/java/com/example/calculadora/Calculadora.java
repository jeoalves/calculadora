package com.example.calculadora;

import java.math.BigDecimal;

public class Calculadora {
    public double somar(double one, double two){
        return (one+two);
    }

    public double subtrair(double one, double two){
        return (one-two);
    }
}
